1st way:

Cloning Method:
git config --list
git clone repourl
git add .
git commit -m"gitcommands added"
git push

After once pushing, if there is any changes:
git add .
git commit -m"gitcommands added"
git push